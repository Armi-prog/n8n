FROM n8nio/n8n:latest

# Установи любой дополнительный плагин, если нужен
# RUN npm install -g <plugin>

# Открытие порта, если будет веб-приложение
EXPOSE 5678

# Укажи точку входа, если хочешь
CMD ["n8n", "start"]
